server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ajeetkumar13/microservice-configuration
          clone-on-start: true
